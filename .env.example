# Sanity project the site reads from. Without these the site falls back to
# lib/placeholder-content.ts.
NEXT_PUBLIC_SANITY_PROJECT_ID=wkwphcl2
NEXT_PUBLIC_SANITY_DATASET=production

# Shared secret for the Sanity publish webhook (see app/api/revalidate).
SANITY_REVALIDATE_SECRET=
